{
  "name": "metplus-pets-production",
  "description": "MetPlus production",
  "keywords": [
  ],
  "scripts": {
    "dokku": {
      "predeploy": "bundle exec rake db:migrate",
      "postdeploy": "VERBOSE=true bundle exec rake db:reset --trace"
    }
  }
}
