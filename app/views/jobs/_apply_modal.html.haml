#applyJobModal.modal.fade{ "aria-labelledby" => "myModalLabel", role: "dialog",
tabindex: "-1" }
  .modal-dialog{ role: "document" }
    .modal-content#job-apply-modal-id
      .modal-header
        %button.close{ "aria-label" => "Close", "data-dismiss" => "modal",
        type: "button" }
          %span{ "aria-hidden" => "true" } ×
        %h4#myModalLabel.modal-title.text-center
          Application process
      .modal-body
        %p
          Job information
          .row
            .col-sm-6
              Job title: #{job.title}
            .col-sm-6
              Job number: #{job.company_job_id}
          .row
            .col-sm-12
              Company:
              = link_to job.company.name, company_home_path(job.company)
          
          .row
            .col-sm-2.warning-text
              Warning:
            .col-sm-10
              This application will send the resume that you have saved
              in your profile to the employer.
              %p
                If you want to make some changes please go to your
                = link_to "profile page", edit_job_seeker_path(pets_user)
                and update the information before following to the next step
          .row
            .col-sm-12
              If you want to proceed with the application press the "Apply button"
      .modal-footer
        = link_to "Apply Now", apply_job_path(job, pets_user), method: :get,
          class: "btn btn-info", id: "modal-apply-id"
        %button.btn.btn-default{ "data-dismiss" => "modal",
          type: "button" } Close
