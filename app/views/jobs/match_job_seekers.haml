%h1
  Job Seeker Matches for job:
  = link_to @job.title, job_path(@job)
%br
- if @job_matches.size > 0
  %table.table.table-hover
    %thead
      %tr.text-center
        %td
          Title
        %td
          Employer
        %td
          Posted
        %td
          Match Rating
    %tbody
      - @list_jobs.each do |job|
        %tr
          %td
            = link_to job.title, job_path(job)
          %td
            = job.company.name
          %td
            #{time_ago_in_words(job.created_at)} ago
          %td
            = show_stars(@star_rating[job.id])
            #{@star_rating[job.id]} / 5.0
- else
  %h4
    No Job Seekers found that match this job
